# ♟️ AI Chess

Petit projet d'IA qui apprend à jouer aux échecs, développé entre amis avec Python et PyTorch.

## ⚙️ Installation
```bash
git clone https://github.com/<ton_nom>/ai-chess.git
cd ai-chess
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
